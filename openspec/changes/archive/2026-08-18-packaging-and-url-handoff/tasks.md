## 1. パッケージング

- [x] 1.1 依存を追加する: `electron-builder`, `electron-updater`
- [x] 1.2 `electron-builder.yml` を作成し、macOS 向け（Apple Silicon / Intel）のビルド設定を書く
- [x] 1.3 アプリのバンドル ID、アイコン、プロダクト名を設定する
- [x] 1.4 バージョニング方針を決めて `package.json` に反映する
- [x] 1.5 ビルドを実行し、`.app` と配布用イメージが生成されることを確認する

## 2. コード署名

- [x] 2.1 Apple Developer 証明書の有無を確認する
- [x] 2.2 証明書がある場合: 署名と公証（notarization）の設定を書く
- [x] 2.3 証明書がない場合: ad-hoc 署名の設定を書き、Gatekeeper 警告などの制約を文書化する
- [x] 2.4 **署名構成の変更で `safeStorage` の復号ができなくなるかを実測する**
- [x] 2.5 復号不能になる場合、ad-hoc から正式署名への移行時の再インポート手順を文書化する
- [x] 2.6 署名済みアプリを起動し、`safeStorage` 参照時に Keychain プロンプトが毎回出ないことを確認する

## 3. 自動更新

- [x] 3.1 更新フィードの配信先を決める（GitHub Releases または自前ホスティング）
- [x] 3.2 `electron-updater` を組み込み、起動時に更新をチェックする
- [x] 3.3 更新フィードへの問い合わせが HTTPS で行われることを確認する
- [x] 3.4 更新パッケージの署名検証を有効にし、検証失敗時は適用しないことを確認する
- [x] 3.5 更新の適用を次回起動時とし、作業中に再起動を強制しないようにする
- [x] 3.6 更新取得失敗時に現行バージョンで動作を継続し、失敗を記録する処理を実装する
- [x] 3.7 ロールバック手順を文書化する

## 4. URL ハンドオフ

- [x] 4.1 `src/main/UrlHandoff.ts` を作成する
- [x] 4.2 カスタムスキーム `aws-console://` を `app.setAsDefaultProtocolClient()` で登録する
- [x] 4.3 `open-url` イベントを処理し、`aws-console://` と `https://` の両経路を共通ハンドラへ集約する
- [x] 4.4 シングルインスタンスロックを取得し、二重起動時は既存ウィンドウへ URL を渡す
- [x] 4.5 アプリ未起動時に受けた URL を、起動完了後に処理するキューを実装する
- [x] 4.6 受理した URL に `NavigationGuard.isAllowed()` を適用し、許可外は `shell.openExternal()` へ返す
- [x] 4.7 URL から `accountId` を抽出するロジックを実装する
- [x] 4.8 既知のアカウントに一致した場合、該当パーティションのタブで開く（未接続なら先に接続する）
- [x] 4.9 `accountId` が抽出できない、または未知の場合はアカウント選択パレットを表示する（**推測で開かない**）
- [x] 4.10 アプリを AWS 系 URL の既定ハンドラとして登録する導線を用意する

## 5. Finicky 連携の文書化

- [x] 5.1 `docs/finicky-integration.md` を作成する
- [x] 5.2 `console.aws.amazon.com` / `awsapps.com/start` / `signin.aws.amazon.com` を本アプリへ流すルール例を記載する（`docs.aws.amazon.com` は含めない）
- [x] 5.3 既定ハンドラ登録の手順を記載する

## 6. 検証

- [x] 6.1 ビルドしたアプリが Apple Silicon で起動することを確認する
- [x] 6.2 署名済みアプリで Keychain プロンプトが起動のたびに出ないことを確認する
- [x] 6.3 バージョンを上げたビルドを配信し、更新が検出・適用されることを確認する
- [x] 6.4 署名検証に失敗する更新が適用されないことを確認する
- [x] 6.5 更新フィードに到達できない状態で起動し、現行バージョンで動作が継続することを確認する
- [x] 6.6 `aws-console://` URL を他アプリから開き、既存ウィンドウのタブで開くことを確認する
- [x] 6.7 `accountId` を含むコンソール URL を開き、該当アカウントのパーティションで開くことを確認する
- [x] 6.8 `accountId` を含まないコンソール URL を開き、アカウント選択パレットが表示されることを確認する
- [x] 6.9 許可外の URL を `aws-console://` 経由で渡し、既定ブラウザへ委譲されることを確認する
- [x] 6.10 Finicky 経由で Slack のリンクから AWS コンソールを開き、本アプリで開くことを確認する
- [x] 6.11 アプリ未起動状態から URL を開き、起動後に該当タブが開くことを確認する
