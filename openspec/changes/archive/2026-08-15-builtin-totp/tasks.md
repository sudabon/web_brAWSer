## 1. 依存とデータモデル

- [x] 1.1 依存を追加する: `otpauth`, `zxing-wasm`
- [x] 1.2 `TotpSeed` 型（`id` / `issuer` / `label` / `secret` / `algorithm` / `digits` / `period`）を定義する

## 2. シードの暗号化保管

- [x] 2.1 `src/main/TotpStore.ts` を作成する
- [x] 2.2 `safeStorage.encryptString()` による `totp.enc` への保存を実装する
- [x] 2.3 `totp.enc` の読み込みと復号を実装する
- [x] 2.4 `safeStorage.isEncryptionAvailable()` が false の場合、保存せず利用者に提示する処理を実装する
- [x] 2.5 復号済みシードをメモリ上にのみ保持する構造にする（ディスクへの書き戻しは暗号化保存時のみ）

## 3. Touch ID ゲート

- [x] 3.1 起動後の初回参照時に `systemPreferences.promptTouchID()` を呼ぶ
- [x] 3.2 認証失敗・キャンセル時はシードを復号せずコードを表示しない
- [x] 3.3 `powerMonitor` の `resume` イベントでメモリ上のシードを破棄し、再ロックする
- [x] 3.4 Touch ID が利用できない環境でのフォールバック方針を決めて実装する

## 4. シードのインポート

- [x] 4.1 `otpauth://totp/{issuer}:{label}?secret=...` のパーサを実装する — `algorithm` / `digits` / `period` は省略時 SHA1 / 6 / 30 を適用する
- [x] 4.2 Base32 の妥当性検証を実装し、不正な入力は登録せずエラーを提示する
- [x] 4.3 `otpauth://` URI の貼り付けによる登録を実装する
- [x] 4.4 Base32 シークレットの直接入力による登録を実装する
- [x] 4.5 `src/main/QrCapture.ts` を作成し、`child_process` から `screencapture -i` を呼ぶ
- [x] 4.6 生成 PNG を `zxing-wasm` でデコードし、`otpauth://` URI を抽出する
- [x] 4.7 デコード完了後に一時 PNG を削除する。削除失敗はエラーとして扱う
- [x] 4.8 デコード失敗時は登録せず、失敗を利用者に提示する
- [x] 4.9 画像ファイルのドラッグ&ドロップによる登録を実装する
- [x] 4.10 Chrome 拡張 Authenticator のバックアップ JSON の実データでスキーマを確認し、一括インポートを実装する

## 5. コード生成と表示

- [x] 5.1 `otpauth` を用いたコード生成を実装する（シードごとの `algorithm` / `digits` / `period` に従う）
- [x] 5.2 `src/renderer/TotpPanel.tsx` を作成し、シードごとに現在のコードと残り秒数リングを表示する
- [x] 5.3 `period` 経過時にコードを自動更新する
- [x] 5.4 コードのクリックでクリップボードへコピーする
- [x] 5.5 コピーから30秒後に、クリップボードの現在値がコピーしたコードと一致する場合のみクリアする
- [x] 5.6 `Cmd+Shift+T` でパネルを開閉するショートカットを登録する

## 6. Identity Center 入力補助

- [x] 6.1 `src/preload/mfa-assist.ts` を作成する
- [x] 6.2 `persist:sso-portal` パーティションかつ Identity Center サインインページの場合のみ動作するガードを実装する
- [x] 6.3 MFA 入力欄の検出ロジックを実装する
- [x] 6.4 入力欄近傍に「入力する」ボタンをオーバーレイ表示する
- [x] 6.5 クリックで現在のコードを入力欄へ挿入する。**フォームの自動送信はしない**
- [x] 6.6 検出失敗時はオーバーレイを出さず、手入力にフォールバックできる状態にする
- [x] 6.7 preload が公開する API を、入力補助に必要な最小の IPC チャネルのみに限定する

## 7. セキュリティ方針の明示

- [x] 7.1 インポート画面に「ルートアカウントおよびブレークグラス用シードは登録しない」方針を明示する
- [x] 7.2 第1要素と第2要素の同居に関するリスクを README に記載する

## 8. 検証

- [x] 8.1 QR 範囲選択からシードを登録し、正しいコードが生成されることを既存 Authenticator と突き合わせて確認する
- [x] 8.2 `otpauth://` URI / Base32 直接入力 / 画像 D&D / Authenticator JSON の各経路で登録できることを確認する
- [x] 8.3 QR 取り込み後に一時 PNG が残っていないことを確認する
- [x] 8.4 アプリのデータディレクトリに Base32 シークレットが平文で現れないことを確認する
- [x] 8.5 起動後の初回参照で Touch ID が要求され、キャンセルするとコードが表示されないことを確認する
- [x] 8.6 スリープ復帰後に再ロックされることを確認する
- [x] 8.7 Identity Center サインインページで「入力する」ボタンが表示され、ワンクリックで挿入され、**自動送信されない**ことを確認する
- [x] 8.8 AWS コンソール上ではオーバーレイが表示されないことを確認する
- [x] 8.9 コピー後30秒でクリップボードがクリアされ、途中で別の内容をコピーした場合は消えないことを確認する
- [x] 8.10 SSO サインインからコンソール接続までがアプリ内で完結することを確認する
