## 1. 永続化基盤

- [x] 1.1 `src/main/PersistenceStore.ts` を作成し、`config.json` / `tabs.json` の読み書きを一元化する
- [x] 1.2 `Tab` 型（`id` / `accountRoleKey` / `url` / `title` / `hibernated` / `lastActiveAt`）を定義する
- [x] 1.3 `sso-account-directory` で導入済みの `config.json` スキーマを拡張する（ハイバネート時間、**サイドパネルの折りたたみ状態と幅**を追加）
- [x] 1.4 タブの追加・削除・ナビゲート時に `tabs.json` を更新する
- [x] 1.5 保存前に federation ログインURL（`SigninToken` を含む URL）を除外する処理を入れる
- [x] 1.6 書き込みの原子性を確保する（一時ファイルへ書いてリネーム）

## 2. ハイバネート

- [x] 2.1 `TabManager` の状態モデルに `hibernated` を追加し、デタッチ（非表示）と区別する
- [x] 2.2 `hibernate(tabId)` を実装する — `webContents` を破棄し、URL / タイトル / favicon のみ保持する
- [x] 2.3 `restore(tabId)` を実装する — 同一パーティションで `webContents` を再生成し、保存 URL へ再ナビゲートする
- [x] 2.4 復元経路を `console-session-lifecycle` の無言再federationと同じ経路に載せる
- [x] 2.5 非アクティブ時間の監視を実装し、既定30分でハイバネートする
- [x] 2.6 ハイバネート時間を設定可能にし、`config.json` に保存する
- [x] 2.7 アカウントあたりのタブ数上限（既定10）を実装し、超過時は最終アクティブ時刻の古い順にハイバネートする（**閉じない**）

## 3. 起動時復元

- [x] 3.1 起動時に `tabs.json` を読み、全タブを `hibernated: true` で復元する
- [x] 3.2 起動時に `webContents` を1つも生成しないことを確認する
- [x] 3.3 サイドパネルのタブセクションに URL / タイトル / favicon を表示する
- [x] 3.4 ハイバネート状態のタブ選択時に `restore` を呼ぶ

## 4. ガードレール（色の可視化）

- [x] 4.1 `preload-aws.ts` にアカウント色バーの注入処理を追加する — `document.documentElement` 上端に高さ6px、`position: fixed` + 高い z-index
- [x] 4.2 メイン → preload へアカウント色を渡す IPC を実装する
- [x] 4.3 SPA 遷移で色バーが消えないよう、DOM 監視による再注入を実装する
- [x] 4.4 色設定の変更が表示中タブへ即時反映されるようにする
- [x] 4.5 サイドパネル左端にアカウント色のアクセントを表示する
- [x] 4.6 ウィンドウのタイトルバーにアカウント色を反映する
- [x] 4.7 `prod` タグ付きアカウントのタブに警告アイコンを表示する
- [x] 4.8 preload が公開する API を「色バー注入」と「MFA 入力補助」の2つのみに限定されていることを確認する

## 5. ダウンロード振り分け

- [x] 5.1 `src/main/DownloadManager.ts` を作成する
- [x] 5.2 各パーティションの `session` に `will-download` ハンドラを登録する
- [x] 5.3 保存先を `~/Downloads/AWS/<accountAlias>/<filename>` に設定する（保存先ダイアログは出さない）
- [x] 5.4 保存先ディレクトリが存在しない場合は作成する
- [x] 5.5 同名ファイルが存在する場合は上書きせず一意な名前を付与する
- [x] 5.6 ダウンロード完了を通知する（ファイル名と保存先を含む）
- [x] 5.7 失敗・キャンセル時はその旨を通知し、完了として扱わない

## 6. ショートカットとコマンドパレット

- [x] 6.1 `src/main/ShortcutRegistry.ts` を作成し、既存のショートカット（`Cmd+Shift+A`, `Cmd+Shift+T`, `Cmd+Opt+I`, `Cmd+Opt+Shift+I`）を集約する
- [x] 6.12 `Cmd+B` にサイドパネルの折りたたみ / 復帰を割り当て、状態と幅を `config.json` に永続化する
- [x] 6.2 重複割当がないことを検証する仕組みを入れる
- [x] 6.3 `Cmd+T`（現在アカウントで新規タブ）、`Cmd+W`（タブを閉じる）を実装する
- [x] 6.4 `Cmd+1`〜`Cmd+9`（現在アカウント内の番号タブ切替）を実装する
- [x] 6.5 `Cmd+Shift+[` / `Cmd+Shift+]`（前後のタブ切替）を実装する
- [x] 6.6 `Cmd+R`（リロード）を実装する
- [x] 6.7 `Cmd+F` を実装する — `webContents.findInPage` によるハイライトと一致件数表示、閉じるとハイライト解除
- [x] 6.8 `src/renderer/CommandPalette.tsx` を作成し、`Cmd+K` でサービス名からコンソールへジャンプできるようにする
- [x] 6.9 コマンドパレットのサービス一覧の供給元を決めて実装する
- [x] 6.10 Escape でパレットをキャンセルできるようにする
- [x] 6.11 `Cmd+Opt+R` のリージョン切替を実装する（コンソールURLのリージョン部分の書き換え）

## 7. 検証

- [x] 7.1 30タブを開いた状態で再起動し、タブが復元され、かつ起動が速いことを確認する
- [x] 7.2 起動直後に `webContents` が生成されていないことを確認する
- [x] 7.3 復元タブを選択して読み込まれ、**再サインインが発生しない**ことを確認する
- [x] 7.4 30分放置したタブがハイバネートされ、メモリが解放されることを確認する
- [x] 7.5 1アカウントで11個目のタブを開き、最も古いタブがハイバネートされる（閉じられない）ことを確認する
- [x] 7.6 アカウント色バーが表示され、コンソールのレイアウトを壊さないことを確認する
- [x] 7.7 コンソールのサービス間遷移後も色バーが維持されることを確認する
- [x] 7.8 `prod` タグのアカウントで警告アイコンが表示されることを確認する
- [x] 7.9 認証情報CSVをダウンロードし、`~/Downloads/AWS/<account>/` に振り分けられることを確認する
- [x] 7.10 同名ファイルの二度目のダウンロードで上書きされないことを確認する
- [x] 7.11 `tabs.json` に `SigninToken` を含む URL が保存されていないことを確認する
- [x] 7.12 全ショートカットが動作し、AWS コンソール自身のショートカットと衝突しないことを確認する
- [x] 7.13 `Cmd+B` で折りたたんだ状態でも、コンテンツ側の上端6px色バーで現在アカウントが判別できることを確認する
- [x] 7.14 折りたたみ状態とパネル幅が再起動後に復元されることを確認する
